#ifndef PALIMNDROME_H
#define PALINDROME_H

#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int is_palindrome(unsigned long n);
unsigned long rev(unsigned long n, unsigned long rever);

#endif /* PALINDROME_H */
